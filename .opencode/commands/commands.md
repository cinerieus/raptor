---
description: RAPTOR commands command
---

Execute the RAPTOR command `/commands $ARGUMENTS`. Read
`.claude/commands/commands.md` completely, treat it as the canonical command
definition, substitute the supplied arguments, and follow its dispatch and
workflow instructions. Do not dispatch any other command unless this command
explicitly requires it.
