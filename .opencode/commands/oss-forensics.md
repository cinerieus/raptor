---
description: RAPTOR oss-forensics command
---

Execute the RAPTOR command `/oss-forensics $ARGUMENTS`. Read
`.claude/commands/oss-forensics.md` completely, treat it as the canonical command
definition, substitute the supplied arguments, and follow its dispatch and
workflow instructions. Do not dispatch any other command unless this command
explicitly requires it.
