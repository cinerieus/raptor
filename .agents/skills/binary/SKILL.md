---
name: binary
description: Run RAPTOR's binary command using its canonical repository definition.
---

# RAPTOR binary

Read `.claude/commands/binary.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$binary` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
