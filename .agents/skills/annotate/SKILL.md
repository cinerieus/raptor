---
name: annotate
description: Run RAPTOR's annotate command using its canonical repository definition.
---

# RAPTOR annotate

Read `.claude/commands/annotate.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$annotate` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
