---
name: web
description: Run RAPTOR's web command using its canonical repository definition.
---

# RAPTOR web

Read `.claude/commands/web.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$web` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
