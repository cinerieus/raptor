---
name: codeql
description: Run RAPTOR's codeql command using its canonical repository definition.
---

# RAPTOR codeql

Read `.claude/commands/codeql.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$codeql` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
