---
name: threat-model
description: Run RAPTOR's threat-model command using its canonical repository definition.
---

# RAPTOR threat-model

Read `.claude/commands/threat-model.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$threat-model` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
