---
name: commands
description: Run RAPTOR's commands command using its canonical repository definition.
---

# RAPTOR commands

Read `.claude/commands/commands.md` completely and follow its dispatch and workflow
instructions. Treat the operator's text following `$commands` as the command arguments.
Do not dispatch any other RAPTOR command unless the canonical definition requires it.
